
        /*
                *  
                *  AllBinary Open License Version 1 
                *  Copyright (c) 2026 AllBinary 
                *   
                *  By agreeing to this license you and any business entity you represent are 
                *  legally bound to the AllBinary Open License Version 1 legal agreement. 
                *   
                *  You may obtain the AllBinary Open License Version 1 legal agreement from 
                *  AllBinary or the root directory of AllBinary's AllBinary Platform repository. 
                *   
                *  Created By: Travis Berthelot    
        */
        
        /* Generated Code Do Not Modify */
        package org.allbinary.util




        import java.lang.Object        
        
        
        import kotlin.Array
        import kotlin.reflect.KClass
        
import java.util.Vector
import jsinterop.annotations.JsType

open public class ABVector<E> : Vector<E> {
        

            //Auto Generated
            public constructor() : super()
            {
            }            
        
    override fun ensureCapacity(minCapacity: Int)
        //nullable = true from not(false or (false and false)) = true
{
var minCapacity = minCapacity
super.ensureCapacity(minCapacity)
}


    override fun capacity()
        //nullable = true from not(false or (false and true)) = true
: Int{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.capacity()
}


    override fun size()
        //nullable = true from not(false or (false and true)) = true
: Int{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.size()
}


    override fun isEmpty()
        //nullable = true from not(false or (false and true)) = true
: Boolean{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.isEmpty()
}


    override fun contains(obj: Any)
        //nullable = true from not(false or (false and false)) = true
: Boolean{
var obj = obj



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.contains(obj)
}


    override fun indexOf(obj: Any)
        //nullable = true from not(false or (false and false)) = true
: Int{
var obj = obj



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.indexOf(obj)
}


    override fun lastIndexOf(obj: Any)
        //nullable = true from not(false or (false and false)) = true
: Int{
var obj = obj



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.lastIndexOf(obj)
}


    override fun elementAt(index: Int)
        //nullable = true from not(false or (false and false)) = true
: E{
var index = index



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.elementAt(index)
}


    override fun firstElement()
        //nullable = true from not(false or (false and true)) = true
: E{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.firstElement()
}


    override fun lastElement()
        //nullable = true from not(false or (false and true)) = true
: E{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.lastElement()
}


    override fun setElementAt(obj: E, index: Int)
        //nullable = true from not(false or (false and false)) = true
{
var obj = obj
var index = index
super.setElementAt(obj, index)
}


    override fun removeElementAt(index: Int)
        //nullable = true from not(false or (false and false)) = true
{
var index = index
super.removeElementAt(index)
}


    override fun addElement(obj: E)
        //nullable = true from not(false or (false and false)) = true
{
var obj = obj
super.addElement(obj)
}


    override fun insertElementAt(obj: E, index: Int)
        //nullable = true from not(false or (false and false)) = true
{
var obj = obj
var index = index
super.insertElementAt(obj, index)
}


    override fun removeElement(obj: Any)
        //nullable = true from not(false or (false and false)) = true
: Boolean{
var obj = obj



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.removeElement(obj)
}


    override fun removeAllElements()
        //nullable = true from not(false or (false and true)) = true
{
super.removeAllElements()
}


    public override fun clone()
        //nullable = true from not(false or (false and true)) = true
: Any{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.clone()
}


    override fun toArray()
        //nullable = true from not(false or (false and true)) = true
: Array<Any?>{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.toArray()
}


    override fun set(index: Int, element: E)
        //nullable = true from not(false or (false and false)) = true
: E{
var index = index
var element = element



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.set(index, element)
}


    override fun add(element: E)
        //nullable = true from not(false or (false and false)) = true
: Boolean{
var element = element



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.add(element)
}


    override fun remove(obj: Any)
        //nullable = true from not(false or (false and false)) = true
: Boolean{
var obj = obj



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.remove(obj)
}


    override fun add(index: Int, element: E)
        //nullable = true from not(false or (false and false)) = true
{
var index = index
var element = element
super.add(index, element)
}


    override fun remove(index: Int)
        //nullable = true from not(false or (false and false)) = true
: E{
var index = index



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.remove(index)
}


    override fun clear()
        //nullable = true from not(false or (false and true)) = true
{
super.clear()
}


    override fun equals(obj: Any?)
        //nullable = true from not(false or (false and false)) = true
: Boolean{
var obj = obj



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.equals(obj)
}


    override fun hashCode()
        //nullable = true from not(false or (false and true)) = true
: Int{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.hashCode()
}


    override fun toString()
        //nullable =  from not(false or (true and true)) = 
: String{



                        //if statement needs to be on the same line and ternary does not work the same way.
                        return super.toString()
}


    override fun removeRange(fromIndex: Int, toIndex: Int)
        //nullable = true from not(false or (false and false)) = true
{
var fromIndex = fromIndex
var toIndex = toIndex
super.removeRange(fromIndex, toIndex)
}


}
                
            

